{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "host:docker:up": "docker compose up",
    "host:docker:down":"docker compose down",
    "container:db:generate": "prisma generate",
    "container:db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "6"
  }
}