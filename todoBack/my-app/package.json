{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^7.4.1",
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^7.4.1"
  }
}